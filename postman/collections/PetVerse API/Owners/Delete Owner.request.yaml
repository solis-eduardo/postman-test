$kind: http-request
url: "{{base_url}}/{{api_version}}/owners/:ownerId"
method: DELETE
pathVariables:
  ownerId: "{{owner_id}}"
scripts:
  - type: afterResponse
    code: pm.test('Owner removido com 204', () => pm.response.to.have.status(204));
    language: text/javascript
order: 4000
