$kind: http-request
url: "{{base_url}}/{{api_version}}/appointments?status=scheduled"
method: GET
queryParams:
  status: scheduled
scripts:
  - type: afterResponse
    code: pm.test('Status 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 1000
