{
    "name": "postman-fern-kit",
    "version": "1.0.0",
    "private": true,
    "description": "Collection Postman (Git Native, schema v3) + spec OpenAPI + Fern Docs, versionadas no repositório.",
    "scripts": {
        "lint": "bash scripts/lint.sh",
        "test": "bash scripts/run-collection.sh Local cli",
        "test:staging": "bash scripts/run-collection.sh Staging cli",
        "export:v2": "bash -c 'set -a; source postman-fern.env; set +a; python3 scripts/export_v2_collection.py && node scripts/normalize_v2_collection.cjs \"dist/$COLLECTION_NAME.postman_collection.json\"'",
        "fern:check": "fern check",
        "fern:dev": "fern docs dev",
        "fern:publish": "fern generate --docs --log-level debug"
    },
    "devDependencies": {
        "postman-cli": "^1.45.0",
        "postman-collection": "^5.3.1",
        "fern-api": "^5.94.0"
    }
}
